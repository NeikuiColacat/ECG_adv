extends:
  - ../experiments/pn2021c_effnet_threechain_locked_official_s5.yaml
  - ../defaults/f005_anchor_geometry_control.yaml

experiment:
  name: f005_anchor_geometry_control_pn2021c_s5
  description: Official S5 consumers for the isolated F005 paired control.

postprocess:
  commands: []

runner:
  name: ${experiment.name}_${matrix.center}_seed${matrix.case.seed}_${matrix.case.variant}
