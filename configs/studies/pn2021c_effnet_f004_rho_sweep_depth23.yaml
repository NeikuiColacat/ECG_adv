extends:
  - ../defaults/pn2021c_official_s5_depth23_composite_protocol.yaml
  - ../defaults/effnet_f004_rho_sweep_locked.yaml
  - ../defaults/f004_pn2021c_reporting.yaml

experiment:
  name: pn2021c_effnet_f004_rho_sweep_depth23
  description: Official S5 depth2+3 known-family evaluation for the independent F-004 rho sweep.

logging:
  child_artifacts:
    - eval_pn2021_c_v7_refexcluded_stream_standard_official_s5_depth23_composite.json

runner:
  matrix:
    center: ${paper_protocol.centers.target_4}
    rho: [0.0, 0.25, 0.5]
  name: ${experiment.name}_${matrix.center}_rho${matrix.rho}
