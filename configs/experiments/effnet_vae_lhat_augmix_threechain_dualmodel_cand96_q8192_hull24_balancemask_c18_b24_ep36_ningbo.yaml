extends:
  - effnet_vae_lhat_augmix_threechain_dualmodel_cand96_q8192_hull24_balancemask_c18_b24_ep36_cpsc.yaml

experiment:
  name: effnet_vae_lhat_augmix_threechain_dualmodel_cand96_q8192_hull24_balancemask_c18_b24_ep36_ningbo
  description: "Ningbo fallback: balanced per-op locked three-chain AugMix with extra random-lead and baseline exposure."

runner:
  matrix:
    center:
      - ningbo

run_record:
  purpose: >-
    Ningbo evidence for the cand96 global fallback recipe. The recipe preserves
    VAE-LH online adversarial training plus locked three-chain AugMix and uses
    the same operator schedule as the CPSC config.
  result_summary: Pending.
  registration_status: provisional
