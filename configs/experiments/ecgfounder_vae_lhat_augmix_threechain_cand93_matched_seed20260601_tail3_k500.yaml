extends:
  - ecgfounder_vae_lhat_augmix_threechain_cand93_matched_seed20260601_k500.yaml

experiment:
  name: ecgfounder_vae_lhat_augmix_threechain_cand93_matched_seed20260601_tail3_k500
  description: ECGFounder cand93-matched trainable-scope ablation with dense plus last three stages trainable.

adaptation:
  run_name: ${matrix.center}_vae_lhat_augmix_threechain_cand93_matched_seed20260601_tail3
  trainable_scope:
    scope: last_n_stages
    last_n_stages: 3

run_record:
  purpose: >-
    Trainable-scope ablation for the cand93-matched ECGFounder replication.
    It tests a wider partial-unfreeze scope than tail2 while preserving the
    same VAE-LH plus locked three-chain AugMix recipe.
  result_summary: Pending.
  registration_status: provisional
