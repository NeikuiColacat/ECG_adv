extends:
  - effnet_vae_lhat_augmix_threechain_dualmodel_cand15_freq_cpsc.yaml

experiment:
  name: effnet_vae_lhat_augmix_threechain_dualmodel_cand16_nosource_anchor_cpsc
  description: "CPSC probe: remove the PTB-XL source-clean stream during online AT while preserving the K500 target clean anchor, VAE-LH hard-sample buffer, and locked three-chain AugMix."

adaptation:
  run_tag_extra: trraw_cand16_nosource_tw25_aw8_c10_b32
  loss:
    ptbxl_weight: 0.0
    target_real_weight: 25.0
    adv_weight: 8.0
    adv_weight_warmup_epochs: 1

run_record:
  purpose: Test whether PTB-XL source-clean batches dilute the VAE-LH adversarial and locked three-chain AugMix signal; K500 target clean anchors remain active as a clean target-center stabilizer.
  result_summary: Pending.
  registration_status: provisional
