extends:
  - effnet_vae_lhat_augmix_threechain_officials5_cand110_weakcombo_cover_c24_strongjsd_b20_ep45_cpsc.yaml

experiment:
  name: effnet_vae_lhat_augmix_threechain_officials5_cand115_weakcombo_latentchain28_c24_b20_aw24_ep45_cpsc
  description: "CPSC pilot: c110 weakcombo schedule with substantially higher VAE-LH adversarial chain mass."

adaptation:
  run_tag_extra: c115officials5_weakcombo_latentchain28_c24_b20_aw24_ep45
  loss:
    adv_weight: 24.0
    adv_weight_warmup_epochs: 6
  latent_augmix:
    chain_weights: "0.36,0.36,0.28"

run_record:
  purpose: >-
    Test the user's hypothesis that the locked AugMix view should allocate
    more mass to the VAE-LH hard-sample waveform instead of mostly raw
    corruption chains. This keeps c110's weak-combo official severity-5
    operator schedule, locked three-chain topology, fixed depth-3 raw
    corruptions, strong JSD/BCE consistency, clean K500 anchors, and local
    same-label VAE latent-hull search, but increases the adversarial AugMix
    chain from 0.10 to 0.28 with a short warmup to reduce clean collapse risk.
    No stabilizer, oracle, single-chain AugMix, raw-supervised extra branch, or
    custom severity profile is added.
  result_summary: Pending.
  registration_status: provisional
