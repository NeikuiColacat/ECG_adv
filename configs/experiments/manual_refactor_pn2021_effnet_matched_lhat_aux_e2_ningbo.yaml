schema_version: 1

experiment:
  name: manual_refactor_pn2021_effnet_matched_lhat_aux_e2_ningbo
  purpose: E2 implementation screen for normalized fixed20 plus one local exact-label LHAT hard endpoint.

entrypoint:
  name: tune_pn2021_direct
  config: train/PN2021_matched_lhat_aux_tune.yaml
  arguments: [--model, efficientnet1dv2, --center, ningbo, --epochs, "2", --scheduler-horizon-epochs, "30"]

output:
  run_dir: /home/linbinhao/ECG_adv_data/runs/agent_workspace/matched_aux_e2/lhat/ningbo
  if_exists: error
  delegate_output_subdir: training
