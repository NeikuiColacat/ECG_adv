extends:
  - effnet_vae_lhat_augmix_threechain_officials5_cand100_depth3_m100_c18_b20_ep45_cpsc.yaml

experiment:
  name: effnet_vae_lhat_augmix_threechain_officials5_cand104_latentchain50_c18_b20_ep45_cpsc
  description: "CPSC pilot: official severity-5 depth-3 locked three-chain AugMix with VAE-LH adversarial chain as the dominant branch."

adaptation:
  run_tag_extra: c104officials5_depth3_m100_latentchain50_c18_b20_ep45
  latent_augmix:
    chain_weights: "0.25,0.25,0.50"

run_record:
  purpose: >-
    Test whether official severity-5 composite recovery improves when the
    VAE-LH hard-sample chain receives dominant AugMix weight. This preserves
    c100's locked three-chain topology, official standard severity-5 raw
    composite branches, JSD/BCE losses, and last-checkpoint policy; it changes
    only the AugMix branch mixture weights from raw/raw/VAE=0.45/0.45/0.10 to
    0.25/0.25/0.50.
  result_summary: Pending.
  registration_status: provisional
