extends:
  - effnet_vae_lhat_augmix_threechain_dualmodel_cand42_noisemask_focus_cpsc.yaml

experiment:
  name: effnet_vae_lhat_augmix_threechain_dualmodel_cand43_noisemask_bcedirect_cpsc
  description: "CPSC probe: combine noise/mask-focused locked three-chain AugMix with stronger supervised BCE on AugMix views."

adaptation:
  run_tag_extra: trraw_proxy_q2048r1_noisemask_bcedir_aw24_tw45_hs10_lam20_c6_cw8_b12
  latent_augmix:
    consistency:
      enabled: true
      consistency_weight: 8.0
      consistency_loss: jsd
      bce_weight: 12.0
      max_batches: 0

run_record:
  purpose: Test whether the useful raw-corruption recovery signal can be internalized through supervised BCE inside the locked three-chain AugMix views, without adding an independent raw-supervised branch.
  result_summary: Pending.
  registration_status: provisional
