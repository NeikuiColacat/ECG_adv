extends:
  - ecgfounder_vae_lhat_augmix_threechain_aligned_effnet_sota_k500.yaml

experiment:
  name: ecgfounder_vae_lhat_augmix_threechain_aligned_effnet_sota_dense_k500
  description: ECGFounder aligned EffNet-SOTA VAE-LH plus locked three-chain AugMix with only the Super5 dense head trainable.

adaptation:
  run_name: ${matrix.center}_vae_lhat_augmix_threechain_aligned_effnet_sota_dense
  trainable_scope:
    scope: dense

run_record:
  purpose: >-
    Dense-only control for the ECGFounder trainable-scope exploration. This is
    not LoRA, but it tests whether preserving the frozen ECGFounder encoder
    avoids the degradation seen under fullFT.
  result_summary: Pending.
  registration_status: provisional
