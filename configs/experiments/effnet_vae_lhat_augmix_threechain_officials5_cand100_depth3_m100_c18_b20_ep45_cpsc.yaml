extends:
  - effnet_vae_lhat_augmix_threechain_officials5_cand99_depthmix_c18_b20_ep45_cpsc.yaml

experiment:
  name: effnet_vae_lhat_augmix_threechain_officials5_cand100_depth3_m100_c18_b20_ep45_cpsc
  description: "CPSC stronger pilot: official severity-5 standard three-chain AugMix with fixed depth-3 and undiluted branch mixture."

adaptation:
  run_tag_extra: c100officials5_depth3_m100_c18_b20_ep45
  latent_augmix:
    depth: 3
    mixture:
      mode: fixed
      prob: 1.0

run_record:
  purpose: >-
    Stress-test whether cand99 is still underexposed to triple composite
    corruptions. This keeps the same locked three-chain VAE-LH AugMix topology
    and official standard severity-5 operator set, but fixes the two raw
    corruption chains to depth 3 and removes clean-anchor dilution inside each
    AugMix view. It is a stronger pilot, not a stabilizer, oracle, single-chain
    AugMix, or raw-supervised branch.
  result_summary: Pending.
  registration_status: provisional
