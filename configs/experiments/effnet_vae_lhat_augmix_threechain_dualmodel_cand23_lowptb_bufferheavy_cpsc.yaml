extends:
  - effnet_vae_lhat_augmix_threechain_dualmodel_cand18_trainproxy_hardvae_cpsc.yaml

experiment:
  name: effnet_vae_lhat_augmix_threechain_dualmodel_cand23_lowptb_bufferheavy_cpsc
  description: "CPSC probe: near-source-free but not source-zero, emphasizing learned hard/AugMix buffer views over repeated clean anchors."

adaptation:
  run_tag_extra: trraw_proxy_lowptb005_buf_aw16_tw55_hs10_lam20_c6_cw16_b6
  loss:
    ptbxl_weight: 0.05
    target_real_weight: 55.0
    adv_weight: 16.0
    adv_weight_warmup_epochs: 2
  latent_augmix:
    consistency:
      enabled: true
      consistency_weight: 16.0
      consistency_loss: jsd
      bce_weight: 6.0
      max_batches: 0

run_record:
  purpose: Test whether a mostly target/buffer training distribution can improve corruption recovery without the instability observed when PTB-XL is removed completely.
  result_summary: Pending.
  registration_status: provisional
