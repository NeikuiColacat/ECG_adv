schema_version: 1

experiment:
  name: manual_refactor_ptbxl_ecgfounder_source_v1
  purpose: Train the whitelist ECGFounder PTB-XL Super5 full-fine-tuned source baseline.

entrypoint:
  name: train_ptbxl_ecgfounder
  config: train/PTBXL.yaml
  # The delegated boot script reads its model-specific defaults from PTBXL.yaml.
  # Add explicit CLI tokens here only for a versioned experiment override.
  arguments: []

output:
  run_dir: /home/linbinhao/ECG_adv_data/runs/manual_refactor/managed_ptbxl_ecgfounder_source_v1
  if_exists: error
  delegate_output_subdir: training
