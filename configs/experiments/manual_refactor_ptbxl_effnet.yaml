schema_version: 1

experiment:
  name: manual_refactor_ptbxl_effnet_source_v1
  purpose: Train the whitelist EfficientNet1DV2 PTB-XL Super5 source baseline.

entrypoint:
  name: train_ptbxl_effnet
  config: train/PTBXL.yaml
  # The delegated boot script reads its model-specific defaults from PTBXL.yaml.
  # Add explicit CLI tokens here only for a versioned experiment override.
  arguments: []

output:
  run_dir: /home/linbinhao/ECG_adv_data/runs/manual_refactor/managed_ptbxl_effnet_source_v1
  if_exists: error
  delegate_output_subdir: training
