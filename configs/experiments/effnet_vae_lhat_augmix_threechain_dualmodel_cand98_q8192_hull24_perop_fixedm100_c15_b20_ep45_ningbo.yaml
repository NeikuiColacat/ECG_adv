extends:
  - effnet_vae_lhat_augmix_threechain_dualmodel_cand97_q8192_hull24_perop_fixedm085_c15_b20_ep45_ningbo.yaml

experiment:
  name: effnet_vae_lhat_augmix_threechain_dualmodel_cand98_q8192_hull24_perop_fixedm100_c15_b20_ep45_ningbo
  description: "Ningbo fallback: cand94 per-op locked three-chain AugMix with fixed AugMix mixture m=1.0."

adaptation:
  run_tag_extra: c98fixedm100_c15_maskpower_aw24_b20_ep45
  latent_augmix:
    mixture:
      mode: fixed
      prob: 1.0

run_record:
  purpose: >-
    Fallback if cand97 still misses the +10 pp recovery gate. This keeps
    VAE-LH online adversarial search plus locked three-chain AugMix, but removes
    the final clean-anchor interpolation inside each AugMix view so the two raw
    ECG corruption chains and one VAE-LH adversarial waveform chain are not
    diluted before being learned into EfficientNet1DV2 parameters.
  result_summary: Pending.
  registration_status: provisional
