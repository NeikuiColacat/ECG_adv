extends:
  - effnet_vae_lhat_augmix_threechain_dualmodel_cand20_trainproxy_shiftmask_cpsc.yaml

experiment:
  name: effnet_vae_lhat_augmix_threechain_dualmodel_cand24_lowptb_shiftmask_cpsc
  description: "CPSC probe: cand20 operator focus with low PTB-XL source dilution and higher buffer pressure."

adaptation:
  run_tag_extra: trraw_proxy_lowptb01_shiftmask_aw12_tw60_hs10_lam18_c8_cw20_b10
  anchors:
    k_anchor: 2400
  hull:
    lambda: 0.18
    steps: 10
    lr: 0.50
  loss:
    ptbxl_weight: 0.1
    target_real_weight: 60.0
    adv_weight: 12.0
    adv_weight_warmup_epochs: 2
  latent_augmix:
    consistency:
      enabled: true
      consistency_weight: 20.0
      consistency_loss: jsd
      bce_weight: 10.0
      max_batches: 0

run_record:
  purpose: Test whether low PTB-XL dilution lets the shift/wander/masking-focused three-chain AugMix recipe transfer into PN2021-C recovery.
  result_summary: Pending.
  registration_status: provisional
