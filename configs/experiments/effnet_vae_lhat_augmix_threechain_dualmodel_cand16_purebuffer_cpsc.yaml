extends:
  - effnet_vae_lhat_augmix_threechain_dualmodel_cand15_freq_cpsc.yaml

experiment:
  name: effnet_vae_lhat_augmix_threechain_dualmodel_cand16_purebuffer_cpsc
  description: "CPSC probe: remove both PTB-XL source-clean and K500 target-clean supervised streams during online AT, leaving the VAE-LH adversarial/AugMix buffer plus locked three-chain AugMix consistency as the training signal."

adaptation:
  run_tag_extra: trraw_cand16_purebuffer_tw0_aw8_c10_b32
  loss:
    ptbxl_weight: 0.0
    target_real_weight: 0.0
    adv_weight: 8.0
    adv_weight_warmup_epochs: 1

run_record:
  purpose: Strict ablation for the user hypothesis that online AT should train only on VAE-LH adversarial samples and locked three-chain AugMix enhanced samples, without PTB-XL or clean K500 supervised streams.
  result_summary: Pending.
  registration_status: provisional
