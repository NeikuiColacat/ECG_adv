extends:
  - ecgfounder_vae_lhat_augmix_threechain_aligned_effnet_sota_k500.yaml

experiment:
  name: ecgfounder_vae_lhat_augmix_threechain_aligned_effnet_sota_tail3_k500
  description: ECGFounder aligned EffNet-SOTA VAE-LH plus locked three-chain AugMix with dense plus last three ECGFounder stages trainable.

adaptation:
  run_name: ${matrix.center}_vae_lhat_augmix_threechain_aligned_effnet_sota_tail3
  trainable_scope:
    scope: last_n_stages
    last_n_stages: 3

run_record:
  purpose: >-
    Test a wider ECGFounder partial-unfreeze scope than tail2 while still
    freezing early foundation blocks and keeping the VAE-LH plus three-chain
    AugMix method unchanged.
  result_summary: Pending.
  registration_status: provisional
