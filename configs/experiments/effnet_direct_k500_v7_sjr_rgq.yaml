extends:
  - ../defaults/effnet1dv2_v7_sjr_rgq_defaults.yaml

experiment:
  name: effnet_direct_k500_v7_sjr_rgq
  description: EfficientNet1DV2 direct K500 historical unmatched artifact replay; retain for provenance, not as the new matched A0.

paper_protocol:
  kshot:
    seed: 20260601
    subset_seed: 20260601

runner:
  entrypoint: ecg_adv_gen/runner/effnet_direct_finetune.py
  adapter: direct_finetune

adaptation:
  method: direct_target_k500_finetune
  comparison_status: historical_unmatched
